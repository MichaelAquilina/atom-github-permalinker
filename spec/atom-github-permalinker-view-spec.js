'use babel';

import AtomGithubPermalinkerView from '../lib/atom-github-permalinker-view';

describe('AtomGithubPermalinkerView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
